import { CollapsedDirective } from './collapsed.directive';

describe('CollapsedDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsedDirective();
    expect(directive).toBeTruthy();
  });
});
